spring.application.name=demo
# --- Configuraci�n de Conexi�n (MariaDB en XAMPP) ---
spring.datasource.url=jdbc:mariadb://localhost:3306/redes_db
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
# --- Configuraci�n de JPA / Hibernate ---
# Muestra el SQL en la consola (�til para ver qu� pasa)
spring.jpa.show-sql=true
# Actualiza la estructura de las tablas autom�ticamente
spring.jpa.hibernate.ddl-auto=update
# Dialecto espec�fico para MariaDB (Optimiza el SQL generado)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect
